{
  "servers": {
    "ssh-inspector": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${workspaceFolder}/dist/ssh-inspector-mcp.mjs",
        "--config",
        "${workspaceFolder}/config.local.json"
      ]
    }
  }
}